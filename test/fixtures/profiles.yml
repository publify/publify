admin:
  label: admin
  nicename: Typo administrator
  modules: <%= [
    :dashboard,
    :write,
    :articles,
    :pages,
    :feedback,
    :themes,
    :sidebar,
    :profile,
    :users,
    :settings,
    :seo,
    :media].to_yaml.inspect %>

publisher:
  label: publisher
  nicename: Blog publisher
  modules: <%= [
    :users,
    :dashboard,
    :write,
    :articles,
    :pages,
    :feedback,
    :media].to_yaml.inspect %>
