<table>
  <tr class="first">
    <th><%= _("Title") %></th>
    <th><%= _("Posts") %></th>
    <th><%= _("Edit") %></th>
    <th><%= _("Delete") %></th>
  </tr>
  <% for category in @categories -%>
  <tr <%= alternate_class -%> id="category_<%= category.id -%>">
    <td><%= link_to_permalink category, category.name %></td>
    <td><%= link_to pluralize(category.articles.size, 'article'), {:controller => 'content', :action => 'list', "search[category]" => category.id} %></td>
    <td class="operation"><%= link_to_edit category %></td>
    <td class="operation"><%= link_to_destroy category %></td>
  </tr>  
  <% end -%>
</table>
