  <% @page_heading = _('Categories') %>
  <% content_for('tasks') do %>
    <%= task_overview %>
  <% end %>
  <% form_tag :action=>"edit", :id => @category.id do %>
  <div class="admin">
    <%= render_partial "form" %>
  </div>

  <p class="paginate r">
     <%= cancel %> <%= _("or") %> <%= save(_("Save")) %> 
  </div>

  <% end %>
