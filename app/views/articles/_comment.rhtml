<li class="graybox" id="comment-<%= comment.id %>">
  <a name="comment-<%= comment.id %>"></a>
  <cite><%= (comment.url.blank?) ? comment.author : link_to(comment.author, comment.url) %> </cite> said <%= distance_of_time_in_words comment.article.created_at, comment.created_at %> later:<br />
  <%= comment.body_html %>
</li>
