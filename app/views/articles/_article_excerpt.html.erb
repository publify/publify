<% if article.excerpt? %>
  <%= article.excerpt %>
  <div class="extended">
    <p><%= link_to_permalink article, _("Continue reading") + "..." %></p>
  </div>
<% else %>
  <%= article.html(:body) %>
  <% if article.extended? %>
    <div class="extended">
      <p><%= link_to_permalink article, _("Continue reading") + "..." %></p>
    </div>
  <% end %>
<% end %>

