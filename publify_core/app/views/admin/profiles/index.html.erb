<% content_for :page_heading do %>
  <h2>
    <%= icon('fas', 'users', class: 'icon-large') %>
    <%= t(".your_profile") %>
  </h2>
<% end %>

<%= form_for([:admin, @user], url: admin_profile_path(current_user)) do |f| %>
  <%= render partial: "admin/users/form", locals: { f: f, user: @user } %>
<% end %>
